<%= form_with(model: log_entry, local: true) do |form| %>
  <% if log_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(log_entry.errors.count, "error") %> prohibited this log_entry from being saved:</h2>

      <ul>
      <% log_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sets %>
    <%= form.number_field :sets %>
  </div>

  <div class="field">
    <%= form.label :reps %>
    <%= form.number_field :reps %>
  </div>

  <div class="field">
    <%= form.label :exercise_name %>
    <%= form.text_field :exercise_name %>
  </div>

  <div class="field">
    <%= form.label :datetime_exercised %>
    <%= form.datetime_select :datetime_exercised %>
  </div>

  <div class="field">
    <%= form.label :current_pain_level %>
    <%= form.number_field :current_pain_level %>
  </div>

  <div class="field">
    <%= form.label :current_pain_frequency %>
    <%= form.text_field :current_pain_frequency %>
  </div>

  <div class="field">
    <%= form.label :progress_note %>
    <%= form.text_area :progress_note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
