<header class='add-log-buttons-container'>
  <%= render 'buttons/new_pt_session' %>
</header>

<% @logs.each do |pt_session| %>
  <%= render partial: 'pt_session', locals: { pt_session: pt_session } %>
<% end %>
<br>

<%= will_paginate @logs, :page_links => false %>
